# -*- mode: python ; coding: utf-8 -*-

block_cipher = None


a = Analysis(['generadorPassword.py'],
             pathex=['C:\\Users\\ingec\\Documents\\03 Cursos y Universidad\\01 Platzi\\12 Curso de python\\Password'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          a.binaries,
          a.zipfiles,
          a.datas,
          [],
          name='generadorPassword',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          upx_exclude=[],
          runtime_tmpdir=None,
          console=True , icon='iniciar-sesion.ico')
